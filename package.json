{
  "devDependencies": {
    "prisma": "^6.14.0",
    "prisma-dbml-generator": "^0.12.0"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0"
  },
  "scripts": {
    "sqlc": "npx prisma migrate diff --from-empty --to-schema-datamodel prisma --script > prisma/migrations/0_init/migration.sql && sqlc generate"
  },
  "prisma": {
    "schema": "./prisma"
  }
}

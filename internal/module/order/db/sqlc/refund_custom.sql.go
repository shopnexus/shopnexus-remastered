// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: refund_custom.sql

package orderdb

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
	null "github.com/guregu/null/v6"
)

const adminDismissDispute = `-- name: AdminDismissDispute :one
UPDATE "order"."refund"
SET "status" = 'Accepted',
    "refund_tx_id" = $1
WHERE "id" = $2 AND "status" = 'Disputed'
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

type AdminDismissDisputeParams struct {
	RefundTxID uuid.NullUUID `json:"refund_tx_id"`
	ID         uuid.UUID     `json:"id"`
}

// AdminDismissDispute: admin sides with the buyer → refund flips
// Disputed → Accepted and the credit ledger leg is recorded.
func (q *Queries) AdminDismissDispute(ctx context.Context, arg AdminDismissDisputeParams) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, adminDismissDispute, arg.RefundTxID, arg.ID)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

const adminUpholdDispute = `-- name: AdminUpholdDispute :one
UPDATE "order"."refund"
SET "status" = 'Rejected',
    "return_to_buyer_transport_id" = $1,
    "rejection_reason" = $2
WHERE "id" = $3 AND "status" = 'Disputed'
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

type AdminUpholdDisputeParams struct {
	ReturnToBuyerTransportID null.Int    `json:"return_to_buyer_transport_id"`
	RejectionReason          null.String `json:"rejection_reason"`
	ID                       uuid.UUID   `json:"id"`
}

// AdminUpholdDispute: admin sides with the seller → refund flips
// Disputed → Rejected and the return-to-buyer transport is recorded so the
// workflow can track delivery of the goods back.
func (q *Queries) AdminUpholdDispute(ctx context.Context, arg AdminUpholdDisputeParams) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, adminUpholdDispute, arg.ReturnToBuyerTransportID, arg.RejectionReason, arg.ID)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

const createBuyerRefund = `-- name: CreateBuyerRefund :one

INSERT INTO "order"."refund" (
    "account_id", "order_id", "reason", "attachments", "return_transport_id"
) VALUES (
    $1, $2, $3, $4, $5
)
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

type CreateBuyerRefundParams struct {
	AccountID         uuid.UUID       `json:"account_id"`
	OrderID           uuid.UUID       `json:"order_id"`
	Reason            string          `json:"reason"`
	Attachments       json.RawMessage `json:"attachments"`
	ReturnTransportID int64           `json:"return_transport_id"`
}

// =============================================
// Module:      order
// File:        refund_custom.sql
// Purpose:     Refund v2 — buyer ships return at create time, seller decides
//
//	within 3D of delivery (auto-accept on timeout), seller may
//	dispute to admin.
//
// =============================================
// Create/Get auto-generated by pgtempl (CreateDefaultRefund / GetRefund).
// CreateBuyerRefund inserts the initial Shipping row with the buyer's evidence
// attachments and the return-leg transport ID already provisioned by the biz
// layer.
func (q *Queries) CreateBuyerRefund(ctx context.Context, arg CreateBuyerRefundParams) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, createBuyerRefund,
		arg.AccountID,
		arg.OrderID,
		arg.Reason,
		arg.Attachments,
		arg.ReturnTransportID,
	)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

const getRefundSnapshotByOrder = `-- name: GetRefundSnapshotByOrder :one
WITH all_refunds AS (
    SELECT "status", "date_created"
    FROM "order"."refund"
    WHERE "order_id" = $1
)
SELECT
    EXISTS (
        SELECT 1 FROM all_refunds
        WHERE "status" IN ('Shipping', 'AwaitingSellerReview', 'Disputed')
    )::BOOLEAN AS has_active_refund,
    COALESCE(
        (SELECT "status" FROM all_refunds ORDER BY "date_created" DESC LIMIT 1) = 'Accepted'::"order"."refund_status",
        false
    )::BOOLEAN AS last_refund_approved
`

type GetRefundSnapshotByOrderRow struct {
	HasActiveRefund    bool `json:"has_active_refund"`
	LastRefundApproved bool `json:"last_refund_approved"`
}

// GetRefundSnapshotByOrder is the per-iteration projection PayoutWorkflow
// reads while watching escrow. has_active_refund flips while any refund is
// in negotiation; last_refund_approved becomes true once the most recent
// refund row for this order lands in Accepted.
func (q *Queries) GetRefundSnapshotByOrder(ctx context.Context, orderID uuid.UUID) (GetRefundSnapshotByOrderRow, error) {
	row := q.db.QueryRow(ctx, getRefundSnapshotByOrder, orderID)
	var i GetRefundSnapshotByOrderRow
	err := row.Scan(&i.HasActiveRefund, &i.LastRefundApproved)
	return i, err
}

const hasActiveRefundForOrder = `-- name: HasActiveRefundForOrder :one
SELECT EXISTS (
    SELECT 1 FROM "order"."refund"
    WHERE "order_id" = $1
      AND "status" IN ('Shipping', 'AwaitingSellerReview', 'Disputed')
) AS has_active
`

// HasActiveRefundForOrder reports whether any refund row for this order is
// still in negotiation (not yet Accepted or Rejected). Used by PayoutWorkflow
// to decide whether to release escrow.
func (q *Queries) HasActiveRefundForOrder(ctx context.Context, orderID uuid.UUID) (bool, error) {
	row := q.db.QueryRow(ctx, hasActiveRefundForOrder, orderID)
	var has_active bool
	err := row.Scan(&has_active)
	return has_active, err
}

const listBuyerRefunds = `-- name: ListBuyerRefunds :many
SELECT id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id FROM "order"."refund"
WHERE "account_id" = $1
ORDER BY "date_created" DESC
LIMIT $3::INTEGER OFFSET $2::INTEGER
`

type ListBuyerRefundsParams struct {
	AccountID   uuid.UUID `json:"account_id"`
	OffsetCount int32     `json:"offset_count"`
	LimitCount  int32     `json:"limit_count"`
}

func (q *Queries) ListBuyerRefunds(ctx context.Context, arg ListBuyerRefundsParams) ([]OrderRefund, error) {
	rows, err := q.db.Query(ctx, listBuyerRefunds, arg.AccountID, arg.OffsetCount, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderRefund{}
	for rows.Next() {
		var i OrderRefund
		if err := rows.Scan(
			&i.ID,
			&i.AccountID,
			&i.OrderID,
			&i.Reason,
			&i.Attachments,
			&i.DateCreated,
			&i.Status,
			&i.ReturnTransportID,
			&i.DateReceivedBySeller,
			&i.ReviewDeadline,
			&i.SellerDecisionAt,
			&i.ReturnToBuyerTransportID,
			&i.RejectionReason,
			&i.RefundTxID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSellerRefunds = `-- name: ListSellerRefunds :many
SELECT r.id, r.account_id, r.order_id, r.reason, r.attachments, r.date_created, r.status, r.return_transport_id, r.date_received_by_seller, r.review_deadline, r.seller_decision_at, r.return_to_buyer_transport_id, r.rejection_reason, r.refund_tx_id FROM "order"."refund" r
JOIN "order"."order" o ON o."id" = r."order_id"
WHERE o."seller_id" = $1
ORDER BY r."date_created" DESC
LIMIT $3::INTEGER OFFSET $2::INTEGER
`

type ListSellerRefundsParams struct {
	SellerID    uuid.UUID `json:"seller_id"`
	OffsetCount int32     `json:"offset_count"`
	LimitCount  int32     `json:"limit_count"`
}

func (q *Queries) ListSellerRefunds(ctx context.Context, arg ListSellerRefundsParams) ([]OrderRefund, error) {
	rows, err := q.db.Query(ctx, listSellerRefunds, arg.SellerID, arg.OffsetCount, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderRefund{}
	for rows.Next() {
		var i OrderRefund
		if err := rows.Scan(
			&i.ID,
			&i.AccountID,
			&i.OrderID,
			&i.Reason,
			&i.Attachments,
			&i.DateCreated,
			&i.Status,
			&i.ReturnTransportID,
			&i.DateReceivedBySeller,
			&i.ReviewDeadline,
			&i.SellerDecisionAt,
			&i.ReturnToBuyerTransportID,
			&i.RejectionReason,
			&i.RefundTxID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markRefundDelivered = `-- name: MarkRefundDelivered :one
UPDATE "order"."refund"
SET "status" = 'AwaitingSellerReview',
    "date_received_by_seller" = CURRENT_TIMESTAMP,
    "review_deadline" = $1
WHERE "id" = $2 AND "status" = 'Shipping'
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

type MarkRefundDeliveredParams struct {
	ReviewDeadline null.Time `json:"review_deadline"`
	ID             uuid.UUID `json:"id"`
}

// MarkRefundDelivered fires when the forward (return) transport reaches its
// final delivered state, flipping the refund into AwaitingSellerReview and
// arming the 3-day auto-accept deadline.
func (q *Queries) MarkRefundDelivered(ctx context.Context, arg MarkRefundDeliveredParams) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, markRefundDelivered, arg.ReviewDeadline, arg.ID)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

const sellerApproveRefund = `-- name: SellerApproveRefund :one
UPDATE "order"."refund"
SET "status" = 'Accepted',
    "seller_decision_at" = CURRENT_TIMESTAMP,
    "refund_tx_id" = $1
WHERE "id" = $2 AND "status" = 'AwaitingSellerReview'
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

type SellerApproveRefundParams struct {
	RefundTxID uuid.NullUUID `json:"refund_tx_id"`
	ID         uuid.UUID     `json:"id"`
}

// SellerApproveRefund transitions AwaitingSellerReview → Accepted and stamps
// the refund_tx_id pointing at the negative ledger leg the biz layer just
// inserted. Same SQL is used for auto-accept on deadline (caller passes the
// system's UUID or NULL for the implicit decider).
func (q *Queries) SellerApproveRefund(ctx context.Context, arg SellerApproveRefundParams) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, sellerApproveRefund, arg.RefundTxID, arg.ID)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

const sellerDisputeRefund = `-- name: SellerDisputeRefund :one
UPDATE "order"."refund"
SET "status" = 'Disputed',
    "seller_decision_at" = CURRENT_TIMESTAMP
WHERE "id" = $1 AND "status" = 'AwaitingSellerReview'
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

// SellerDisputeRefund transitions AwaitingSellerReview → Disputed when the
// seller refuses the refund. A companion row is inserted into refund_dispute
// in the same biz call.
func (q *Queries) SellerDisputeRefund(ctx context.Context, id uuid.UUID) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, sellerDisputeRefund, id)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

const withdrawBuyerRefund = `-- name: WithdrawBuyerRefund :one
UPDATE "order"."refund"
SET "status" = 'Cancelled'
WHERE "id" = $1
  AND "account_id" = $2
  AND "status" = 'Shipping'
RETURNING id, account_id, order_id, reason, attachments, date_created, status, return_transport_id, date_received_by_seller, review_deadline, seller_decision_at, return_to_buyer_transport_id, rejection_reason, refund_tx_id
`

type WithdrawBuyerRefundParams struct {
	ID        uuid.UUID `json:"id"`
	AccountID uuid.UUID `json:"account_id"`
}

// WithdrawBuyerRefund cancels a refund while it is still in Shipping.
// Once the goods reach the seller (AwaitingSellerReview), withdraw is no
// longer possible — biz layer rejects the call before reaching this SQL.
func (q *Queries) WithdrawBuyerRefund(ctx context.Context, arg WithdrawBuyerRefundParams) (OrderRefund, error) {
	row := q.db.QueryRow(ctx, withdrawBuyerRefund, arg.ID, arg.AccountID)
	var i OrderRefund
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.OrderID,
		&i.Reason,
		&i.Attachments,
		&i.DateCreated,
		&i.Status,
		&i.ReturnTransportID,
		&i.DateReceivedBySeller,
		&i.ReviewDeadline,
		&i.SellerDecisionAt,
		&i.ReturnToBuyerTransportID,
		&i.RejectionReason,
		&i.RefundTxID,
	)
	return i, err
}

.vscode/

# Per-machine compose overrides — the embedding service's key and tunnel address.
.env

internal/config/*.yml
!internal/config/config.example.yml
docs/
legacy/
tmp/

# Bytecode from dev/spec-audit; the scripts are the artefact, not their cache.
__pycache__/
*.pyc
